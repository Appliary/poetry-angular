.form(ng-controller='mathFormula/editor')
    h4 {{ 'inputs:' + $root.__module.name + ':title' | translate }}
    table.table
        thead: tr
            th {{ 'varName:' + $root.__module.name + ':col' | translate }}
            th {{ 'device:' + $root.__module.name + ':col' | translate }}
            th {{ 'measurement:' + $root.__module.name + ':col' | translate }}
            th {{ 'timeOffset:' + $root.__module.name + ':col' | translate }}
            th {{ 'currentValue:' + $root.__module.name + ':col' | translate }}
            th &nbsp;
        tbody
            tr( ng-repeat=' (i,input) in item.inputs ')

                //- Name of the variable
                td: b {{ input.varName }}

                //- Device name / Tags
                td {{ input.kind.slice(0,1) }}&nbsp;
                    em(ng-if="input.kind == 'tags'")
                        | {{ input.id }}
                    em(ng-if="input.kind != 'tags'")
                        a(ng-href="/deviceManager/{{input.kind}}/{{input.id}}" target="_blank")
                            | {{ input.id }}

                //- Value type
                td {{ input.type }}&nbsp;
                    em(ng-if="input.indice !== undefined") {{ input.indice }}

                //- Time dimension
                td(ng-if='input.time')
                    | {{ input.time }} {{ 'days:unit'|translate}}
                td.muted(ng-if='!input.time')
                    | {{ 'last:time:formula' | translate }}

                //- Example value
                td.muted( ng-if=" !inputValues[ input.varName ].forEach " )
                    | {{ inputValues[ input.varName ] }}
                td.muted( ng-if=" inputValues[ input.varName ].forEach ", ng-click="showVals(inputValues[ input.varName ])" )
                    | [ {{ inputValues[ input.varName ][0] }},… ]

                //- Delete btn
                td.text-right: a.btn.btn-circle.red(ng-click='inputs.remove(i)')
                        i.fa.fa-trash
                        
            tr
                td( colspan="4" ) &nbsp;
                td.text-right( colspan="2" )
                    a.btn.btn-circle.green(ng-click='inputs.add()')
                        i.fa.fa-plus

    h4(ng-if="formulaInput") {{ 'formula:' + $root.__module.name + ':title' | translate }}
    .form-body(ng-class='{error: currentOutputErr}', ng-if="formulaInput")
        .form-group
                textarea.form-control( ng-model='item.formula', rows="3" )
        .form-group
                input.form-control( disabled, ng-value='currentOutput' )
    
    //- div(ng-if="!formulaInput")
        label(for="timeout")
        input(ng-model="item.timeout", required, type="number")

    .form-actions.text-right(style='background:0 0;')
        button.btn.green(ng-click='save()', ng-disabled='currentOutputErr')
            | {{ 'save:' + $root.__module.name + ':button' | translate }}

//- span.saved( ng-if=' item.__saved ' )
        | {{ 'saved:' + $root.__module.name | translate }}
    span.failed( ng-if=' item.__failed ' )
        | {{ 'failed:save:' + $root.__module.name | translate }}