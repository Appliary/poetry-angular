//- OLD
    header.router-header
        h1(icon="{{$root.__module.icon}}")
            | {{ $root.__appName + ':application:header' | translate }}
            | &gt;
            | {{ $root.__module.name + ':module:title' | translate }}
        .toolbox
            button( ng-repeat='( name, config ) in $root.__module.config.toolbox', ng-click="openToolbox( name )")
                | {{ name + ':toolbox:' + $root.__module.name | translate }}

    ng-include(src='$root.__module.templateUrl')
    
.page-bar
    ul.page-breadcrumb
        li
            span {{ $root.__appName + ':application:header' | translate }}
            i.fa.fa-circle
        li
            span {{ $root.__module.name + ':module:title' | translate }}

    .page-toolbar
        .btn-group.pull-right
            button.btn.green.btn-sm.btn-outline.dropdown-toggle(type='button', data-toggle='dropdown', data-hover='dropdown', data-delay='1000', data-close-others='true')
                | Actions 
                i.fa.fa-angle-down
            ul.dropdown-menu.pull-right(role='menu')
                li(ng-repeat='( name, config ) in $root.__module.config.toolbox', ng-click="openToolbox( name )")
                    a(href='#')
                        i.icon-bell 
                        |  {{ name + ':toolbox:' + $root.__module.name | translate }}
                
ng-include(src='$root.__module.templateUrl')
