h4 Overview

ng-include.row( src='"generic/form.pug"', ng-controller='alertsList/overview' )

.col-xs-12
    .btn-toolbar.pull-right
        button.btn.btn-sm.red( ng-click='confirmDeletion()', ng-if=' !!confirmDeletion ' )
            | {{ 'delete:button' | translate }}
        button.btn.btn-sm.custom(ng-click=' save() ', ng-if="!disableSaveBTN")
            | {{ 'save:' + $root.__module.name + ':button' | translate }}

.last.col-xs-12
    .btn-toolbar.pull-right
        //- un
        button.btn.btn-sm.default( ng-if="item.acknowledged" ng-click='unacknowledge(item._id)' )
            | {{ 'unacknowledge:alerts:button' | translate }}
        //- acknowledge
        button.btn.btn-sm.default( ng-if="!item.acknowledged" ng-click='acknowledge(item._id)' )
            | {{ 'acknowledge:alerts:button' | translate }}
        //- un
        button.btn.btn-sm.default( ng-if="item.read" ng-click='unread(item._id)' )
            | {{ 'unread:alerts:button' | translate }}
        //- read
        button.btn.btn-sm.default( ng-if="!item.read" ng-click='read(item._id)' )
            | {{ 'read:alerts:button' | translate }}
