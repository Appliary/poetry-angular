div(ng-controller='videoCtrl')
  .row
    label(for='fileToUpload') Enter Youtube's url
    form
        input(type="text",ng-model="video.url")
    .modalButtons
        button.btn.btn-primary.pull-right(ng-click="apply()")
            | {{ 'Save:chart' | translate }}
